---

---

<section class="help" id="services">
  <div class="help-inner">
    <p class="section-label reveal">How I can help</p>
    <h2 class="help-heading reveal reveal-d1">Three ways to work with me</h2>

    <div class="help-grid">
      <a href="https://appsecco.com" target="_blank" rel="noopener" class="help-card reveal reveal-d1">
        <span class="help-number">01</span>
        <h3 class="help-title">Test your product security</h3>
        <p class="help-desc">Appsecco tests your apps, APIs, cloud infrastructure, and AI integrations. You get a report your engineering team can act on — not a compliance checklist.</p>
        <span class="help-link">appsecco.com &nearr;</span>
      </a>

      <a href="https://kloudle.com" target="_blank" rel="noopener" class="help-card reveal reveal-d2">
        <span class="help-number">02</span>
        <h3 class="help-title">Scan your cloud for misconfigurations</h3>
        <p class="help-desc">Kloudle finds cloud security issues before your AI agents exploit them. Built for dev teams that don't have a dedicated security person.</p>
        <span class="help-link">kloudle.com &nearr;</span>
      </a>

      <a href="/#contact" class="help-card reveal reveal-d3">
        <span class="help-number">03</span>
        <h3 class="help-title">Train your team on AI security</h3>
        <p class="help-desc">From pentesting to AI red teaming — workshops and training programs for security teams adapting to the agent era.</p>
        <span class="help-link">Get in touch &darr;</span>
      </a>
    </div>
  </div>
</section>

<style>
  .help { padding: 6rem var(--gutter); }

  .help-inner { max-width: var(--max-w); margin: 0 auto; }

  .help-heading {
    font-size: clamp(1.75rem, 3.5vw, 2.75rem);
    margin-bottom: 3rem;
    color: var(--color-text);
  }

  .help-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1px;
    background: var(--color-border);
    border: 1px solid var(--color-border);
  }

  .help-card {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    padding: 2rem;
    background: var(--color-surface);
    color: var(--color-text);
    transition: background 0.15s;
  }
  .help-card:hover { background: var(--color-surface-hover); color: var(--color-text); }

  .help-number {
    font-family: var(--font-mono), ui-monospace, monospace;
    font-size: 0.7rem;
    font-weight: 600;
    color: var(--color-accent);
    letter-spacing: 0.05em;
  }

  .help-title {
    font-family: var(--font-sans), system-ui, sans-serif;
    font-size: 1.15rem;
    font-weight: 600;
    line-height: 1.3;
  }

  .help-desc {
    font-size: 0.95rem;
    color: var(--color-text-dim);
    line-height: 1.75;
    flex: 1;
  }

  .help-link {
    font-family: var(--font-mono), ui-monospace, monospace;
    font-size: 0.8rem;
    color: var(--color-text-dim);
    opacity: 0.5;
    transition: all 0.15s;
  }
  .help-card:hover .help-link { opacity: 1; color: var(--color-accent); }
</style>
